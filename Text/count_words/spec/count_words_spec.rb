require 'spec_helper'

describe CountWords do
  it 'has a version number' do
    expect(CountWords::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
